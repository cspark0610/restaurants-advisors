import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    height: 150,
    width: '100%',
    resizeMode: 'contain',
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
  },
});
